<?xml version="1.0" encoding="utf-8"?>
<!-- 自定义主 Manifest（Custom Main Manifest）。注意：切勿保留 "GENERATED BY UNITY" 注释，
     否则下次构建 Unity 会用模板覆盖本文件、把 VIBRATE 权限丢掉。
     本文件在 Unity 2022.3 标准主 Manifest 基础上增加 VIBRATE 权限，保证 NativeHapticDriver 真机震动生效。
     须包含 UnityPlayerActivity，否则构建报 "Unable to find Unity activity in manifest"。-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application>
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:theme="@style/UnityThemeSelector"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>
    </application>
</manifest>
